# Build/run artifacts — never committed. pnpm-lock.yaml is deliberately NOT
# ignored (it is the integrity-pinned source of truth; the go.sum analog).
node_modules/
# Authenticated storage state written by global-setup; session cookie, never committed.
/.auth/
/test-results/
/playwright-report/
/blob-report/
/.cache/
*.log
